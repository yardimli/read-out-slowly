OPEN_ROUTER_API_KEY="your_open_router_api_key"
OPENAI_API_KEY="your_openai_api_key"

# Optional: Default model if not specified in function call
DEFAULT_LLM_FOR_SIMPLE_HELPER="google/gemini-2.5-pro-preview-03-25"
OPENAI_TTS_MODEL="tts-1" # or tts-1-hd

# Paths - adjust if your structure is different
# These are relative to the script that runs the helper
APP_URL="http://localhost:8000" # For referer header, and constructing file URLs
LOG_DIRECTORY="storage/logs"
PUBLIC_STORAGE_PATH="public" # Base for publicly accessible files
FFMPEG_PATH="ffmpeg" # Or full path like /usr/bin/ffmpeg if not in PATH

RECAPTCHA_V3_SITE_KEY=""
RECAPTCHA_V3_SECRET_KEY=""
RECAPTCHA_V3_SCORE_THRESHOLD=""
